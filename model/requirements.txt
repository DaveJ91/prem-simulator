# Core deps for fetch_data + dixon_coles + generate_odds (default flow).
# Install: pip install -r model/requirements.txt
#
# For the Bayesian model (Tier 3), also install requirements-bayesian.txt
# — separated because pymc + arviz pull in ~500MB and aren't needed for
# the default Dixon-Coles + xG path.

requests>=2.31
pandas>=2.0
lxml>=4.9          # for pandas.read_html
beautifulsoup4>=4.12

scipy>=1.11        # MLE for Dixon-Coles
numpy>=1.26
